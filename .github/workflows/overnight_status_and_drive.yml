name: Overnight Status Report

on:
  schedule:
    # GitHub scheduled workflows can be delayed or dropped under load. Keep this
    # idempotent and stagger retries so the morning ops report is available by 5am ET.
    - cron: "30 7 * * *"
    - cron: "15 8 * * *"
    - cron: "45 8 * * *"
  workflow_dispatch:

jobs:
  overnight-status:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build overnight status report
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PYTHONPATH: .
        run: python scripts/overnight_status_report.py

      - name: Set ET report date
        run: echo "ET_REPORT_DATE=$(TZ=America/New_York date +%F)" >> "$GITHUB_ENV"

      - name: Note
        run: echo "Drive push disabled; Relay.app now handles downstream distribution."
